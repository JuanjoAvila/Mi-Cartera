# feat: quitar UI de ordenar brókers en Herramientas de inversión

## Qué se borró

- **`14-v4-screens.js`**: IIFE autoejecutable que pintaba las flechas ▲▼ (botones "al inicio" / "al fondo") y los inputs numéricos en la hoja lateral "Herramientas de inversión". Todo eliminado.
- **`06-sync-brokers.js`**: eliminación de `secOrderOf(state, 'cartera_brokers', groupsBase.map(...))`. Ahora usa orden fijo: revolut → trade_republic → myinvestor (respetando el filter de posiciones). Ya no lee del state.
- **`01-i18n.js`**: borradas `v4_broker_order` + `v4_broker_order_h` en los 3 idiomas (es/en/ca).

## Build

```
npm run build → ✅ public/index.html (1159 KB, 15 módulos)
```

-72 lines eliminadas, +8 insertadas. El bundle bajó de 1160 a 1159 KB.

## Testing recomendado

- Abre Cartera → Inversiones: se ven los 3 brókers en orden fijo.
- Herramientas (⚙): ya no muestra controles de orden ni sección "Orden de los brókers".
- El resto del modal Cartera funciona igual.
- Playwright E2E en `src/modules` para asegurar que no roto nada.
