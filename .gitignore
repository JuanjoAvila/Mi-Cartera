# Dependencias
node_modules/
# Logs
*.log
npm-debug.log*
# Secretos — NUNCA commitear
.env
.env.*
secrets.json
*.key
# Sistema
.DS_Store
Thumbs.db
# Editor
.vscode/
.idea/
*.swp
# Artefactos temporales de build local
/tmp/
/supabase/.temp/
# Bundle local de la app Android (lo genera scripts/build-www.mjs desde public/)
/www/
# APKs generadas localmente para compartir (build/outputs ya está gitignored por Android;
# esta es la copia suelta en la raíz para pasar por WhatsApp/Drive, no es código del repo)
*.apk
# Tooling local de Claude (preview launch.json, settings locales) — no es código de la app
.claude/
# Playwright
playwright-report/
test-results/
# Capturas del vídeo de referencia de Revolut (pantalla REAL del usuario: nombre, IBAN…).
# Repo público → JAMÁS se commitean (AGENTS §8). Referencia local para diseño solamente.
/docs/design/handoff/capturas/revolut-profile-pull/
/docs/design/handoff/capturas/video-frames/
/docs/design/handoff/capturas/video-frames-dense/
# ⚠ Y NINGÚN VÍDEO, VENGA DE DONDE VENGA. La regla de arriba solo cubría las carpetas de
# capturas, así que el 17/7 se coló `revolut-settings-swipe.mp4` en el commit 202fd4e: 11 s de
# su Revolut REAL con su nombre completo, su IBAN, su foto y su @usuario, en un repo PÚBLICO.
# Un vídeo de referencia se mira en local; al repo no entra nunca.
*.mp4
*.mov
*.webm
# Scratch de depuración nativa local (logcat, capturas sueltas de adb screencap, fotogramas
# extraídos con ffmpeg) — herramientas de sesión, no artefactos del repo.
/.gradle-local/
