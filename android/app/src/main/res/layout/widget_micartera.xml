<?xml version="1.0" encoding="utf-8"?>
<!-- Widget "gasto del mes": lo pinta MiCarteraWidget con RemoteViews (solo vistas soportadas). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/w_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="14dp">

    <TextView
        android:id="@+id/w_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_title"
        android:textColor="#9FB3A8"
        android:textSize="11sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/w_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="0 €"
        android:textColor="#5FD08A"
        android:textSize="26sp"
        android:textStyle="bold" />

    <ProgressBar
        android:id="@+id/w_bar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="6dp"
        android:progressTint="#5FD08A"
        android:progressBackgroundTint="#1E2B24"
        android:max="100"
        android:progress="0" />

    <TextView
        android:id="@+id/w_sub"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textColor="#E8F0EB"
        android:textSize="12sp" />

    <!-- «Lo que te puedes permitir» (gasto seguro): la cifra que de verdad importa al mirar el widget. -->
    <TextView
        android:id="@+id/w_afford"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text=""
        android:textColor="#5FD08A"
        android:textSize="15sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <TextView
        android:id="@+id/w_foot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text=""
        android:textColor="#9FB3A8"
        android:textSize="11sp" />
</LinearLayout>
