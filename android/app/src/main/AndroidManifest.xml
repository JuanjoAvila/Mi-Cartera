<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep-link micartera:// — la vuelta del login del banco (Open Banking): bank-callback
                 sirve una página puente que navega a micartera://bank?ok=1 (o ?msg=...). MainActivity
                 lo guarda como "goto" y la web lo consume al volver (mismo mecanismo que las notis). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="micartera" />
            </intent-filter>

        </activity>

        <!-- Lector de la notificación de gasto de Trade Republic (reemplazo de MacroDroid).
             Requiere activar "Acceso a notificaciones" para Mi Cartera en Ajustes de Android. -->
        <service
            android:name=".TrExpenseListener"
            android:label="Mi Cartera · captura de gastos"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Widget de pantalla de inicio: gasto del mes vs presupuesto. Lo alimentan
             la web (plugin MiCartera.updateWidget) y el lector de notis de TR. -->
        <receiver
            android:name=".MiCarteraWidget"
            android:label="Mi Cartera · gasto del mes"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_micartera_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Huella/candado del móvil para el desbloqueo (plugin MiCartera.bioVerify) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Notificaciones locales (Android 13+): confirmación de gasto capturado y avisos de presupuesto -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Botón «Actualizar app»: descargar el APK nuevo y abrir el instalador (sin cable) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
</manifest>
